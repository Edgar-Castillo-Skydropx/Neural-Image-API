/**
 * Tipos de activación disponibles para las capas neuronales
 */
export enum ActivationType {
  SIGMOID = 'sigmoid',
  RELU = 'relu',
  TANH = 'tanh',
  SOFTMAX = 'softmax',
  LINEAR = 'linear'
}
